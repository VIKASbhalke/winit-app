<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.winit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".coloreccanActivity"></activity>
        <activity android:name=".lungcanActivity" />
        <activity android:name=".prostratecanActivity" />
        <activity android:name=".brecancerActivity" />
        <activity android:name=".skincancerActivity" />
        <activity android:name=".setnotificationActivity" />
        <activity android:name=".contactActivity" />
        <activity android:name=".feedbackActivity" />
        <activity android:name=".donateActivity" />
        <activity android:name=".calendarActivity" />
        <activity android:name=".FaqActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>